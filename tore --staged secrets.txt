[1mdiff --git a/s1.md b/s1.md[m
[1mindex 73c640f..6530700 100644[m
[1m--- a/s1.md[m
[1m+++ b/s1.md[m
[36m@@ -4,6 +4,8 @@[m
 [m
 #### Рабочий каталог — текущая версия кода[m
 [m
[32m+[m[32m#### Ничего, кроме исходников, не должно попадать в репозиторий[m
[32m+[m
 - `git init` — создать пустой репозиторий[m
 - `git init --bare` — создать пустой репозиторий без рабочего каталога[m
 - `git clone <url>` — склонировать репозиторий в новый каталог[m
[1mdiff --git a/s2.md b/s2.md[m
[1mindex b0b6ad4..45bc872 100644[m
[1m--- a/s2.md[m
[1m+++ b/s2.md[m
[36m@@ -2,17 +2,6 @@[m
 [m
 #### История версий — дерево коммитов[m
 [m
[31m-#### Индекс коммита используется для постепенной сборки коммита[m
[32m+[m[32m#### Коммиты неизменны, а хэш — идентификатор коммита[m
 [m
[31m-- `git add .` — добавить все измененные файлы в индекс[m
[31m-- `git add -p <filename>` — выбрать изменения для добавления в индекс[m
[31m-- `git commit -m <msg>` — сохранить содержимое индекса в репозиторий в виде коммита[m
[31m-- `git commit -am <msg>` — добавить изменения в уже известных файлах в индекс, затем сохранить содержимое индекса в репозиторий в виде коммита[m
[31m-- `git status -sb` — вывести состояние каталога и индекса кратко с указанием текущей ветки[m
[31m-- `git restore .` — отменить изменения каталога в соответствии с индексом[m
[31m-- `git restore -S .` — отменить изменения индекса в соответствии с коммитом (отмена git add .)[m
[31m-- `git rm <filename>` — удалить файл из каталога и индекса, чтобы перестать хранить его историю в репозитории[m
[31m-- `git show <commit>` — показать содержимое коммита[m
[31m-- `git diff <from_commit> [<to_commit>]` — вывести разницу между двумя коммитами[m
[31m-- `git diff --name-status <from_commit> [<to_commit>]` — список измененных файлов[m
[31m-- `git difftool <from_commit> [<to_commit>]` — вывести разницу с помощью difftool из настроек[m
\ No newline at end of file[m
[32m+[m[32m#### Ничего, кроме исходников, не должно попадать в репозиторий[m
\ No newline at end of file[m
